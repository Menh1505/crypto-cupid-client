import React from 'react';
import Navbar from '../components/navbar';

const Home: React.FC = () => {
    return (
        <div>
            <Navbar />
            <h1 style={{ alignItems: 'center', justifyContent: 'center', display: 'flex' }}>Home</h1>
            <h1 style={{ alignItems: 'center', justifyContent: 'center', display: 'flex' }}>I don't know what to put here yet</h1>
        </div>
    );
};

export default Home;